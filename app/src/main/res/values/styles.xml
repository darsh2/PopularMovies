<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="CardViewStyle" parent="android:Widget">
        <item name="android:paddingTop">@dimen/padding_widget</item>
        <item name="android:paddingRight">@dimen/padding_widget</item>
        <item name="android:paddingBottom">@dimen/padding_widget</item>
        <item name="android:paddingLeft">@dimen/padding_widget</item>
    </style>
</resources>
